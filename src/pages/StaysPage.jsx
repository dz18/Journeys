import { Typography, Container } from "@mui/material";

export default function StaysPage() {
    return (
        <Container>
            <Typography>Stays Page</Typography>
        </Container>
    )
}